import React from 'react';export default function Home({account}){return (<div className='home-page'><section className='hero'><h2>Welcome {account.name}</h2><p className='muted'>Streamer-only 1v1 / 2v2 matches and 10-player tournaments.</p><div className='cta'><button className='btn primary' onClick={()=>alert('Go to Play to request a match')}>Play vs Streamer</button><button className='btn' onClick={()=>{const r=JSON.parse(localStorage.getItem('cw_requests')||'[]');r.push({id:'r_'+Math.random().toString(36).slice(2,9),name:account.name,mode:'1v1'});localStorage.setItem('cw_requests',JSON.stringify(r));alert('Request sent')}}>Quick Request</button></div></section><aside className='home-side'><div className='panel'>Your name: {account.name}</div><div className='panel'>Rules: one account per device. Name is permanent.</div></aside></div>)}